# الاختبارات

```powershell
.venv\Scripts\python.exe -m pytest -q
.venv\Scripts\python.exe -m mkdocs build --strict
```

تغطي الاختبارات: سلامة البيانات، عدم تسرب مجموعات الخصائص بين التدريب والاختبار، إعادة تدريب النموذج الرباعي، استخراج NLP متعدد الأعراض والنفي، أولوية قواعد الطوارئ، تطابق أمثلة الأمراض الثمانية، رفض أمثلة خارج النطاق، سلامة التوليد العشوائي، وتكامل واجهة Tkinter.
