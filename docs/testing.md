# الاختبارات

```powershell
.venv\Scripts\python.exe -m pytest -q
.venv\Scripts\python.exe -m mkdocs build --strict
```

تغطي الاختبارات: سلامة البيانات، عدم تسرب مجموعات الخصائص بين التدريب والاختبار، إعادة تدريب النموذج الرباعي، استخراج NLP متعدد الأعراض والنفي، أولوية قواعد الطوارئ، الحالات التجريبية الثلاث، وتكامل واجهة Tkinter.
